# The sample files are evidence-style test data (some contain deliberate invisible characters).
# Keep them byte-for-byte identical on every operating system: no line-ending conversion.
samples/** -text
